<div id="page_wrapper">
	<h1>new msg</h1>
	<%= form_for :post, url: posts_path do |f| %>
		<%= if @post.errors.any? %>
			<div id="errors">
				<h2><%= pluralize(@post.errors.count, "error") %> preneted this post form saving: </h2>
				<ul>
					<% @post.errors.full_messages.each do |msg| %>
						<li> <%= mgs %> </li>
					<% end %>
				</ul>
			</div>
		<% end %>
		<%= f.label :title %><br>
		<%= f.text_field :title %><br>
		<br>
		<%= f.label :body %><br>
		<%= f.text_field :body %><br>
		<br>
		<%= f.submit %>
	<% end %>
</div>